<Page x:ConnectionId='1'
    x:Name="pageRoot"
    x:Class="AriaView.Model.MapPage"
    DataContext="{Binding  ViewModel, RelativeSource={RelativeSource Self}}"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:AriaView.Model"
    xmlns:common="using:AriaView.Common"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"                         >

    <Page.Resources>
        <!-- TODO: Delete this line if the key AppName is declared in App.xaml -->
        <x:String x:Key="AppName">AriaView</x:String>
    </Page.Resources>
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition/>
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="140"/>
            <RowDefinition Height="55"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Back button and page title -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="120"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button x:Name="backButton" Margin="39,59,39,0" Command="{Binding NavigationHelper.GoBackCommand, ElementName=pageRoot}"
                        Style="{StaticResource NavigationBackButtonNormalStyle}"
                        VerticalAlignment="Top"
                        AutomationProperties.Name="Back"
                        AutomationProperties.AutomationId="BackButton"
                        AutomationProperties.ItemType="Navigation Button"/>
            <TextBlock x:Name="pageTitle" Text="{StaticResource AppName}" Style="{StaticResource HeaderTextBlockStyle}" Grid.Column="1" 
                        IsHitTestVisible="false" TextWrapping="NoWrap" VerticalAlignment="Bottom" Margin="0,0,30,40"/>
        </Grid>
        <Grid Grid.Row="1" Height="auto" Width="auto">
            <Button x:ConnectionId='2' x:Name="previousTermBtn" HorizontalAlignment="Left"  Content="previous"                               Width="auto" Height="auto"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Width="auto">
                <TextBlock x:Name="termDateTB" Height="auto" Width="auto" Text="{Binding currentTermName}"/>
                <ComboBox x:ConnectionId='4' x:Name="dateTermsCB"  Header="Term" ItemsSource="{Binding AriaViewDate.DateTerms}"  Height="auto" Width="auto"                                                >
                    <ComboBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Orientation="Vertical" />
                        </ItemsPanelTemplate>
                    </ComboBox.ItemsPanel>
                </ComboBox>
                <ComboBox x:ConnectionId='5' x:Name="datesCB" Header="Date" Width="auto" ItemsSource="{Binding datesList}"                                            />
            </StackPanel>
            <Button x:ConnectionId='3' x:Name="nextTermBtn" HorizontalAlignment="Right" Content="next"                           Width="auto" Height="auto"/>
        </Grid>
        <local:MapView x:Name="mapView" Height="Auto" Width="Auto"  Grid.Row ="2"/>
    </Grid>
</Page>

